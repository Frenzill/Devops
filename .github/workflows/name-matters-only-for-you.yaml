on: push
jobs:
   if-Windows-else:
      runs-on: ubuntu-latest
      steps:
        - name: condition 1
          if: runner.os == 'Windows'
          run: echo "Это $env:RUNNER_OS."
        - name: condition 2
          if: runner.os != 'Windows'
          run:  echo "Это не  винда, а $env:RUNNER_OS."
