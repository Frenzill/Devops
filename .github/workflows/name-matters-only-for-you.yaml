on: push
jobs:
  build:
   if-Windows-else 
      runs-on: ubuntu-latest
      steps:
        - name: condition 1
          if runner.os == 'Windows'
          run: echo "Это винда"
        - name: condition 2
          if runner.os != 'Windows'
          run:  echo "Это не  винда"
