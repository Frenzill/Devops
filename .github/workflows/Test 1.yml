name: test
on:
 push:
    
  pull_request:

  workflow_dispatch:

jobs:
  step:
    # The type of runner that the job will run on
    runs-on: ubuntu-latest

    steps:
      
      - uses: actions/checkout@v3
